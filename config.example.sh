# Configuration de Claude Setup.
# Copie ce fichier en config.local.sh et adapte-le. config.local.sh est ignoré par Git,
# donc tes valeurs restent locales et personnelles.

# Identité git imposée à l'install, pour que tes commits soient toujours à ton nom.
# Laisse vide pour ne pas toucher à ta config git existante.
SETUP_GIT_NAME=""
SETUP_GIT_EMAIL=""
